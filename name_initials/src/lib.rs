pub fn initials(names: Vec<&str>) -> Vec<String> {
    let v : Vec(String) = Vec::new();

    for name in names{
        let namesplit = name.split(" ")
        for n in namesplit{
            
        }

    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
